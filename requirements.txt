# Network Device Manager — Python 3.8+
# Install with: pip install -r requirements.txt

# ── Core (required) ───────────────────────────────────────────────────────────
# Credential encryption at rest
cryptography>=3.4

# ── Network connectivity (required for real SSH operations) ───────────────────
# SSH session management for Cisco IOS/IOS-XE and Huawei VRP
netmiko>=4.0

# ── Testing ───────────────────────────────────────────────────────────────────
# Run: python -m pytest tests/ -v  (or: python -m unittest discover -s tests -v)
pytest>=7.0        ; extra == "dev"

# ── Standard-library modules (no install needed — listed for documentation) ───
# tkinter   — GUI
# json      — data persistence
# logging   — application logs
# socket    — TCP connectivity checks
# pathlib   — file system operations
# datetime  — timestamps
# threading — async/background operations
# ipaddress — CIDR/range parsing
# difflib   — configuration comparison
# re        — compliance rule matching
# enum      — device status states
# typing    — type hints
